VITE_API_URL=/api
VITE_APP_NAME="KU SHEET"
VITE_APP_VERSION="1.0.0"

# Google OAuth (Optional) — ใส่ค่าในไฟล์ .env ของคุณเองเท่านั้น ห้าม commit คีย์จริง
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Google Maps (Optional) — ใส่ค่าในไฟล์ .env ของคุณเองเท่านั้น ห้าม commit คีย์จริง
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# หมายเหตุ:
# - ไฟล์นี้เป็นตัวอย่างเท่านั้น (example). โปรดสร้างไฟล์ .env ของคุณเองและเติมคีย์จริง
# - ระบบมี .gitignore เพื่อกันไม่ให้ .env ถูก commit แต่ถ้าเคยถูก track มาก่อน
#   สามารถรัน: git rm --cached frontend/.env  เพื่อหยุดติดตามไฟล์ดังกล่าว